const score = 4000 
const balance = new Number(400) // here both are number but in another we explicitly definr a number 
// console.log(score.toFixed(2)) // use  for precison value in e-commerce value 
const money  = 1.9854
// console.log(money.toLocaleString("en-IN")); // it convert ACOORIDNG TO INDIAN RUPPR SYSYTEM
// console.log(money.toPrecision(3))
///////////++++++++ MATHS+++++++++++++++++//////////
console.log(Math)  
// it is an object 

console.log (Math.random() ) // gives random value between 0 and 1 ;